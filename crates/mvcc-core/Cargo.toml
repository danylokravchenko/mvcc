[package]
name = "mvcc-core"
description = "Core types and traits for the mvcc crate: timestamps, isolation levels, the Versioned trait."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme = "README.md"
keywords = ["mvcc", "database", "transactions", "concurrency", "isolation"]
categories = ["database-implementations", "concurrency"]

[dependencies]

[lints]
workspace = true
